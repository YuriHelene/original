<%= stylesheet_link_tag 'edit', :media => "all" %>

<div class="back-visual" style="background-image: url(<%= asset_path('bg-proedit1.jpg') %>);">

<h3>投稿編集</h3>

<div class="post-container">
<%= form_for @tweet do |f| %>
<div class="field">
  <h2><%= f.label :タイトル %></h2>
  <%= f.text_field :title,class: "input-title", :size => 140 %> 
  
  <h2><%= f.label :投稿内容 %></h2>
  <%= f.text_area :body, class: "input-body", :size => 140 %> 
  
  <h2><%= f.label :画像 %></h2>
  <% if @tweet.image.attached? %>
  <p>現在の画像:</p>
  <div class="current-image">
    <%= image_tag @tweet.image.variant(resize_to_limit: [250, 250]), class: "current-image-preview" %>
  </div>
  <% else %>
    <p>画像は未設定です</p>
  <% end %>
  <%= f.file_field :image %>

  <h2>ハッシュタグ</h2>
    <p>現在のタグ: <%= @tweet.hashtags.map(&:hashtag_name).join(" ") %></p>
    <%= f.text_field :hashtag_name, value: @tweet.hashtags.map(&:hashtag_name).join(" "), placeholder: "夕焼け 虹 朝" %>
    <p>*半角スペースを入力すると複数のタグを付けることができます。</p> 
  </div>
<%# <% end %> 

<%# <%= form_for @tweet do |f| %> 
  <%# <div class='form-group'> %>
      <%# <%= f.collection_check_boxes(:tag_ids, Tag.all, :id, :name) do |tag| %> 
          <%# <div class='form-check'> %>
              <%# <%= tag.label class: 'form-check-label' do %> 
                  <%# <%= tag.check_box class: 'form-check-input' %> 
                  <%# <%= tag.text %> 
              <%# <% end %> 
          <%# </div> %>
      <%# <% end %> 

  <%= f.submit "編集する" %>
<% end %>
</div>